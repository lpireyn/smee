# Smee hooks - Debug

These hooks print debug messages to stdout, as well as a few useful environment variables and files.

## Activation

Activate these hooks with:

``` shell
git config set --global --append smee.hooks debug
```
