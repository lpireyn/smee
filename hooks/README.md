# Smee hooks

This directory is a library of various hooks intended to be used with Smee.

## Installation

Clone this repository and register this directory with Smee:

``` shell
git config set --global --append smee.dirs DIR/hooks
```

where `DIR` is the directory where the repository was cloned.
