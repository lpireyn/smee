# Smee hooks - `notify-send`

These hooks display a notification after some operations, using `notify-send`.

## Activation

Activate these hooks with:

``` shell
git config set --global --append smee.hooks notify-send
```
