# Smee hooks - cargo/fmt-check

These hooks run `cargo fmt --check` to validate a commit.

## Activation

Activate these hooks with:

``` shell
git config set --local --append smee.hooks cargo/fmt-check
```
